; Trust the local cache instead of revalidating every package with the registry
; on each install. Lockfile integrity hashes still gate what gets used.
prefer-offline=true

; No engine-strict here on purpose: Dependabot's npm updater runs Node 20, so
; enforcing engines.node at install time fails every update it opens.
