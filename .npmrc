; Trust the local cache instead of revalidating every package with the registry
; on each install. Lockfile integrity hashes still gate what gets used.
prefer-offline=true

; Refuse to install on a Node version outside `engines.node`, so a mismatch is
; reported against this repo rather than surfacing as a warning about a dependency.
engine-strict=true
